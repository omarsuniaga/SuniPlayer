[build]
  publish = "app/"

[build.processing]
  # skip_processing = true  # uncomment if you need raw passthrough

# SPA redirect — todas las rutas caen al index.html
[[redirects]]
  from = "/*"
  to   = "/index.html"
  status = 200
