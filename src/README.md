# Legacy Root Src

This root-level `src/` directory is no longer the primary application source.

Official active app paths:

- `apps/web/src`
- `apps/native/src`
- `packages/core/src`

Anything still left under this root `src/` should be treated as transitional or legacy material unless a specific migration task says otherwise.
